This repo is the web version of the netrunner card game. Your job is to help manage the conversion from clojure/clojurescript to typescript.

If asked to do a conversion, you must make sure that all functionality is faithfully ported to typescript. Where a library is specific to clojure, use an equivalent library in typescript. Do not analyze the code for improvements, read the source and destination files then do your best to copy the clojure functionality to typescript. If typescript code already exists, validate that each function is as close as possible to the clojure version and fix any differences.

Do not read other files if it can be avoided.

Do not return a response after you finish all writes. Do not use any git commands
