Fix the typescript and any other errors for all files in src/ts/game/cards
Fix the typescript and any other errors for all files in src/ts/game/core

Fix any typescript errors in the identities files from the clojure conversion in src/ts/game/cards/identities.ts
Fix any typescript errors in the assets files from the clojure conversion in src/ts/game/cards/assets.ts
Fix any typescript errors in the events files from the clojure conversion in src/ts/game/cards/events.ts
Fix any typescript errors in the hardware files from the clojure conversion in src/ts/game/cards/hardware.ts
Fix any typescript errors in the operations files from the clojure conversion in src/ts/game/cards/operations.ts
Fix any typescript errors in the agendas files from the clojure conversion in src/ts/game/cards/agendas.ts

Check every file in src/clj/game/core has been converted to an equivalent typescript file in src/ts/game/core. CHECK EVERY FILE ONE BY ONE. Some conversions were poor quality and will need to be fixed, so make sure that every function is present and fully implemented with no stubs and no errors.

Check every file in src/clj/game/cards has been converted to an equivalent typescript file in src/ts/game/cards. CHECK EVERY FILE ONE BY ONE. Some conversions were poor quality and will need to be fixed, so make sure that every function is present and fully implemented with no stubs and no errors.

Check every file in src/clj/game has been converted to an equivalent typescript file in src/ts/game. CHECK EVERY FILE ONE BY ONE. Some conversions were poor quality and will need to be fixed, so make sure that every function is present and fully implemented with no stubs and no errors.

Done:

Check:
